<UserControl x:Class="ChatApp.UserControls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="50" Margin="10">
        <TextBlock Text="1.0.0V" FontSize="20" Foreground="Black" Margin="0" FontFamily="Comic Sans MS"/>
        <TextBlock Text="ChatApplication" FontFamily="Comic Sans MS" FontWeight="ExtraBlack" Foreground="Black" FontSize="20" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,8,0">
            <Button x:Name="btnMinimize" Style="{StaticResource MinimizeButtonStyle}" Click="btnMinimize_Click" />
            <Button x:Name="btnMaximize" Style="{StaticResource MaximizeButtonStyle}" Click="btnMaximize_Click"  Margin="10,0,0,0"/>
            <Button x:Name="btnExit" Style="{StaticResource ExitButtonStyle}" Click="btnExit_Click" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
